# -*-python-*-
# Time-stamp: <02/10/06 00:32:34 smulloni>
# $Id: hoptime.conf,v 1.1 2002/10/06 14:37:59 smulloni Exp $

# this configuration file is meant to be sourced
# with an Include directive by sw.conf; it supports
# the Hoptime application.

for x in ('requestHandler',
          'web',
          'templating',
          'auth',
          'rewrite',
          'postgresql',
          'hoptime.service'):
    if x not in services:
        services.append(x)

PostgreSQLConnectParams = {'hoptime' : 'localhost:hoptime:jsdds:Mamjjasond'}

Scope(Location("/hopwerk/",
               authAuthorizer="hoptime.hopauth.CookieHoptimeAuth",
               authAuthorizerCtorArgs=('auth',
                                       'hopwerk',
                                       {'path':'/hopwerk/'},
                                       '/login.html'),
               authActivated=1))

# add rewrite rules here
